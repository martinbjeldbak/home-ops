---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ${APP}-garage-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: ${APP}-garage-secret
    template:
      data:
        ACCESS_KEY_ID: "{{ .GARAGE_ACCESS_KEY_ID }}"
        ACCESS_SECRET_KEY: "{{ .GARAGE_SECRET_ACCESS_KEY }}"
        # Garage validates the SigV4 region against garage.toml's s3_region ("garage")
        REGION: "garage"
  dataFrom:
    - extract:
        key: garage
