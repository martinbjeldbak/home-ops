---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: acestream-http-proxy
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: acestream-http-proxy
  values:
    controllers:
      acestream-http-proxy:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/martinbjeldbak/acestream-http-proxy
              tag: 3.3.0@sha256:84d66745f1dacc93f332307df61406e4f418c74980c23564117ef5d7cf6765c5
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 100m
              limits:
                memory: 256Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: 192.168.69.131
        ports:
          http:
            port: &port 6878
    route:
      app:
        hostnames: ["acestream.martinbjeldbak.com"]
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    persistence:
      cache:
        type: emptyDir
        globalMounts:
          - path: /.cache
      ace-database:
        type: emptyDir
        sizeLimit: 2Gi # engine's own --cache-limit caps the stream cache at 1GB; this is a node-side backstop
        globalMounts:
          - path: /home/appuser/.ACEStream
