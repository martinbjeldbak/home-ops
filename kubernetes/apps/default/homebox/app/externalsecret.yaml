---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homebox
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: homebox-secret
    template:
      data:
        HBOX_MAILER_HOST: "{{ .SMTP_RELAY_HOSTNAME }}"
  dataFrom:
    - extract:
        key: smtp-relay
