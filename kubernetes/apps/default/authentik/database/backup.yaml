---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: authentik-backup
spec:
  schedule: "0 0 0 * * *"  # midnight
  immediate: true # test backup
  backupOwnerReference: self
  cluster:
    name: authentik-database
