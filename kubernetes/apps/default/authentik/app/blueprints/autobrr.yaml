---
# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
version: 1
metadata:
  name: Autobrr OAuth2 Provider and Application
context:
  authorization_flow: &authorization_flow !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
  invalidation_flow: &invalidation_flow !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
  property_mappings: &property_mappings
    - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
    - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
    - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
  signing_key: &signing_key !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
entries:
  - model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: Provider for Autobrr
    attrs:
      name: Provider for Autobrr
      authorization_flow: *authorization_flow
      invalidation_flow: *invalidation_flow
      client_type: confidential
      client_id: !Env AUTOBRR_CLIENT_ID
      client_secret: !Env AUTOBRR_CLIENT_SECRET
      redirect_uris:
        - url: https://autobrr.martinbjeldbak.com/api/auth/oidc/callback
          matching_mode: strict
      property_mappings: *property_mappings
      signing_key: *signing_key

  - model: authentik_core.application
    identifiers:
      slug: autobrr
    attrs:
      name: Autobrr
      slug: autobrr
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, Provider for Autobrr]]
      policy_engine_mode: any
      meta_icon: https://cdn.jsdelivr.net/gh/selfhst/icons@main/svg/autobrr.svg
      meta_description: Automation for torrent trackers and usenet indexers
      meta_publisher: autobrr
      open_in_new_tab: false
