---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-trakt-sync-cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
  storageClassName: ceph-block
